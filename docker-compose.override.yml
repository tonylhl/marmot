version: "3"
services:
  mysql:
    build: ./docker/marmot-mysql
    restart: always
    volumes:
      - $HOME/marmot_home/mysql_data:/var/lib/mysql
  web:
    build: .
    restart: always
    depends_on:
      - mysql
    ports:
      - "80:9900"
    environment:
      - MYSQL_HOST=mysql
      - MARMOT_HOST=127.0.0.1 # replace it with your hostname: MARMOT_HOST=example.com
      - MARMOT_PORT=80        # keep the same as ports' left number
  nginx:
    build: ./docker/marmot-nginx
    restart: always
    volumes:
      - $HOME/marmot_home/static:/usr/share/nginx/html:ro
    ports:
      - "9920:80"
